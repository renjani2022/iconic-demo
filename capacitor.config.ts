import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-demo-4',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
